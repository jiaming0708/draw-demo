import { LevelBlockDirective } from './level-block.directive';

describe('LevelBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new LevelBlockDirective();
    expect(directive).toBeTruthy();
  });
});
